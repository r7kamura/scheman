CREATE TABLE `table1` (
  `column1` INTEGER NOT NULL AUTO INCREMENT,
  `column2` VARCHAR(255) NOT NULL,
  PRIMARY KEY (`column1`)
);

CREATE TABLE `table2` (
  `column1` INTEGER NOT NULL AUTO INCREMENT,
  PRIMARY KEY (`column1`)
);
